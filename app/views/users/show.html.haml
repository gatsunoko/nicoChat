.user_show.list_index.list_show
  %h2{class: 'username w100 text-center'}
    = @user.name
  - if user_signed_in? && @user.id == current_user.id
    .user_edit_link.w100.text-center
      = link_to 'ユーザー名編集', edit_user_path(current_user.id), class: 'btn btn-primary'
  %h3
    作成したチャットルーム
  %ul.rooms
    - @rooms.each do |room|
      %li.room
        = link_to room_path(room) do
          %p
            = room.title

